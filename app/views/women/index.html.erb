<div class="page-title">
  Female Models
</div>
<div class="models">
  <% @women.each do |w| %>
    <%= render partial: 'shared/model', locals: { model: w } %>
    <%= link_to('Edit', model_edit_path(w), class: 'btn') %>
  <% end %>
</div>